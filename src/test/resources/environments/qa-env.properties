# Environment configuration
environment=qa
baseUrl=https://qaplayground.com/bank/login

# Credentials are provided externally through environment variables
username=${username}
password=${password}

# Common synchronization timeout (seconds)
timeout=10
#browserName=${browserName}